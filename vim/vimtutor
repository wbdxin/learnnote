VIMTUTOR -1.7
主要介绍部分命令,足够使得vim作为通用编辑器
学习方法:实践

1.1:移动光标  
            ^
            k
   <h               l>  
            j
            v
注意:要在normal模式下移动,可以通过按ESC键来回到normal模式.

1.2 退出VIM  
	1.按<ESC>来确保退回到normal模式.  
	2.输入: :q!<Enter>;会丢失所作的修改并退出vim.
	3.输入: wq!<Enter>;会写入所作的修改并退出vim

1.3 文本编辑 - 删除
在normal模式下,可以按下x键来删除光标所在位置的字符
	1.在normal模式下,移动光标到待删除字符.
	2.按x键进行删除.

1.4 文本编辑 - 插入
	在normal模式下,可以按下i键来插入文本.
	1.在normal模式下,将光标移动到待插入字符的位置.
	2.按i键并后续输入要插入的字符.
	3.插入后按ESC,返回normal模式

1.5 文本编辑 - 添加
	1.在normal模式,移动光标到待添加的行.
	2.按A,并后续输入要添加的字符.
	3.在输入完成后,按ESC退回到normal模式
1.6 编辑文件
	使用:wq以保存文件并推出

小结:
	1.移动光标可以用方向键,也可以用hjkl
	2.进入vim,执行vim命令
	3.推出vim,在normal模式:q!或者:wq
	4.在normal模式下,按x键删除光标所在字符.
	5.在normal模式下,请输入:
      i，后跟要插入文本 <ESC>;在在光标前插入
      A，后跟要插入文本 <ESC>;在行末添加文本

2.1删除类命令
	输入dw可以从光标处删至词尾,包括空格
	1.在normal模式,移动光标到单词首字母
	2.输入dw,从光标处删除至词尾
	3.光标移至下一单词首字母
2.2更多删除类命令
	输入d$可以从光标处删至行尾
	1.确保在normal模式
	2.移动光标到待删首字符
	3.输入d$,删除光标至行尾字符
2.3操作码和对象
	修改文本的命令由操作码和对象构成
	删除命令d的格式如下:
	d 对象
	d-是操作码
	对象包括:
		w 到下一个单词头,会删除下一单词前的空格
		e 到本单词尾,不会删除下一单词前的空格
		$ 到行尾,包含最后一个字符
		0 到行首,包含空格
		^ 到行首,不包含空格
		G 到文本最后一行
		gg 到文本首行
注:不按操作码按下对象键时,仅会按对象指定的方式移动光标
2.4对对象使用计数
	在对象前键入重复的次数
	2 w,当前光标向后移动2个单词,移动到第三个单词首字符
2.5使用计数以删除的更多
	d 2 w,删除当前光标所在单词及下一个单词,光标移到第三单词的首字符
2.6行操作
	dd删除光标当前所在行
	2 dd删除光标所在行开始的2行
2.7恢复命令
	u 恢复最后执行的命令
	U 恢复行的原始状态
	Ctrl+R 撤销恢复命令
小结:
	dw,删除光标到下一个单词
	d$,删除光标到行尾
	dd,删除整行
	2w,预置数字以重复操作对象
	0,移动到行首
	u,恢复单词修改
	U,恢复对整行的修改
	Ctrl-R,恢复恢复

3.1PUT命令
	输入p以在光标处粘贴先前删除的文本
	1.移动光标到待移动的行
	2.dd,删除整行
	3.移动光标到文本要移动到的位置
	4.按p粘贴
3.2替换命令
	输入r(字符),以(字符)代替光标所在的字符
3.3变更操作码
	键入ce,改变光标到单词末尾(不含空格)
3.4其他的c更改命令
	与删除命令一样,使用相同的对象
	c number motion
	w,e,$,
	修改后按ESC退回normal模式
小结:
	p,粘贴刚删除的文本到光标当前为止
	r+char,使用char替代光标所在字符
	c number motion,改变光标所在到motion的字符

4.1光标定位和文件状态
	Ctrl+G,显示当前光标位置和状态
	G,移动光标到文件行尾
	number G,移动到文件的第number行
4.2搜索命令
	1.在normal模式,键入字母/,光标出现在屏幕底部
	2.输入要搜索的字符串,按enter匹配第一个结果
	3.键入n继续同方向查找,N反方向继续查找
	4.要反向搜索字符串,用?代替/
	5.ctrl+o,返回上次位置
	6.ctrl+i,向前移动
4.3括号匹配搜索
	输入%,以找到对应的),],}
4.4替换命令
	输入:s/old/new/g <enter>,在同一行中使用new替换old
	输入:s/old/new <enter>,使用new代替一次old
	输入:#,#s/old/new/g,指定更改的行数范围
	输入:%s/old/new/g,全文范围替换
	输入:%s/old/new/gc,全文范围内替换,并给出是否的提示
小结:
Ctrl-G,显示当前行数和状态
G,移动到末尾
n G,移动到n行
gg,移动到文件头
/+s，搜索s
?+s,反向搜索s
n，同方向继续搜索
N，反方向继续搜索
Ctrl-o，返回上次位置
ctrl-i，下次位置
%，搜索匹配的括号
:s/old/new,用new替换old一次
:s/old/new/g,用new替换一行中的所有old
#,#s/old/new/g,在#,#行范围内用new替换所有old
%s/old/new/g,全文中替换
%s/old/new/gc，全文中替换，并提前确认
